<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.etiscanmotus">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Storage.ApplicationPrefs"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EtiScanMotus"
        android:usesCleartextTraffic="true">
        <activity android:name=".EtiScan.EtiScan_Scan_Check"></activity>
        <activity android:name=".NewScan.NewScan" />
        <activity android:name=".EtiScan.EtiScan_Scan" />
        <activity android:name=".EtiScan.EtiScan" />
        <activity android:name=".login.Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>